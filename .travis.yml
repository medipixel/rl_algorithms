language: python
python:
    - "3.6"

service:
    - docker

before_install:
    - docker run -it medipixel/reinforcement_learning_examples
    - docker ps -a

# install:
#     - make dev
#     - make dep

script:
    # run static analysis and format check
    - make test

cache:
  directories:
    ~/docker-images
