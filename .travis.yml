language: python
python:
    - "3.6"

service:
    - docker

install:
    - docker build . -t rl-examples-test

# install:
#     - make dev
#     - make dep

script:
    # run static analysis and format check
    - docker run rl-examples-test make test
