version: 2.1

jobs:
  build-and-test:
    docker:
        - image: medipixel/rl_algorithms
    steps:
      - checkout
      - run:
          name: Install requirements
          command: make dev
      - run:
          name: Test
          command: make test

workflows:
  main:
    jobs:
      - build-and-test
